name: vars contexts

on : 
  workflow_dispatch:
env:
  my_username: ${{vars.username}}
jobs:
  builds:
    runs-on: ubuntu-latest
    steps:
     - name: "Display Info"
       run: echo "my username is ${{env.my_username}}"
    
   
